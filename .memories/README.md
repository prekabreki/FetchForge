# Memories index

Project knowledge store: one fact per file, grep-friendly, committed and shared.
Each file opens with `description:` frontmatter. This index is auto-generated by
`tools/memory-index.py` (run by the pre-commit hook) -- do not hand-edit the list.

- [editable-install-live-serving](editable-install-live-serving.md) -- Deploying/merging/syncing while a FetchForge job runs -- the editable install serves index.html from disk per-request, so a page reload after a git file change mismatches the still-in-memory old backend
- [libplacebo-custom-shader-silent-noop](libplacebo-custom-shader-silent-noop.md) -- mpv .hook shaders in libplacebo (custom_shader_path) fail SILENTLY -- LUMA hooks no-op on RGB input and FSRCNNX self-disables below 1.3x scale -- so argv assertions can "pass" while the shader does nothing; also there is NO built-in libplacebo sharpen option
- [local-testing-gotchas](local-testing-gotchas.md) -- Testing this app locally -- the heartbeat watchdog kills the server mid-curl, and how to exercise the frontend JS headlessly (jsdom, no browser)
- [project-overview](project-overview.md) -- What FetchForge is, how it runs, and the load-bearing constraints
- [scale-cuda-sharpen-runtime-verified](scale-cuda-sharpen-runtime-verified.md) -- scale_cuda + cas sharpen branch of _decode_filter_args verified end-to-end on real Windows NVENC hardware (issue #34) -- upscale, upscale+sharpen, and same-size passthrough+sharpen all produce correct-resolution, correct-frame-count, cleanly-decoding output, and cas measurably alters pixels
- [scaler-filter-frame-domain](scaler-filter-frame-domain.md) -- When appending a CPU filter (cas sharpen) after a GPU scaler in _decode_filter_args, libplacebo needs NO hwdownload (it outputs software frames) but scale_cuda DOES (it outputs CUDA frames) -- mixing them up crashes ffmpeg with -38
